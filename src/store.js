
import {Observable, of } from 'rxjs';


// const streamA$ = Observable.of(2);
// const streamB$ = Observable.of(4);
// const streamC$ = Observable.concat(streamA$, streamB$)
//   .reduce((x, y) => x  + y);

// streamC$.subscribe(console.log);
